/**
 * Every source — a word or an image — is normalised to this height in the
 * internal coordinate space. That is what lets one slider value mean the same
 * thing across a five-letter word, a twenty-letter word and a photograph.
 */
export const UNIT_HEIGHT = 200;
